package navigation

// Main navigation package - provides memory management

import "core:mem"
import "core:log"

// Navigation memory system
nav_memory_init :: proc() {
    // No-op for now - memory is managed by Odin's allocator
}

nav_memory_shutdown :: proc() {
    // No-op for now
}